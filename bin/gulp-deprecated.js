#!/usr/bin/env node
'use strict';

const path = require('path');
console.log(process.env.WEBPACK_OPTS)
console.log('we are no longer using gulp as our buildtool for sho.com. Try `npm run distribute`, or consult package.json')